# Enclave Python deps — EXACT-version pinned, including transitive deps, because
# a floating dependency moves PCR0. A given version's wheel filename is immutable
# on PyPI, so exact pins yield byte-identical installs across builds; combined
# with PYTHONDONTWRITEBYTECODE + the mtime clamp in the Dockerfile, the python
# layer is deterministic.
#
# `requirements.lock` carries the SHA-256 hashes used by the Docker build.
# Regenerate it on the aarch64 build host with `./build.sh lock`.
#
# Target wheels: cp3x-abi3-manylinux_2_28_aarch64 (AL2023 / Graviton).
cryptography==43.0.1
cffi==1.17.1
pycparser==2.22
